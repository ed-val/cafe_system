import Menu from './menu';

export default class sortOptimized {
  constructor(props) {
    // super(props);
    this.state = {
      baristaOneFreeAt: 0,
      baristaTwoFreeAt: 0,
      time: 0,
    }
    console.log('OPTIMIZED', Menu);
  }

  sortOrders(oders) {
    console.log(orders);
  }
}